# Week 1 needs only four packages. Resist installing more —
# numpy/soxr/webrtcvad arrive in later weeks when the course needs them.
fastapi
uvicorn[standard]
openai          # used against the xAI endpoint (base_url=https://api.x.ai/v1)
python-dotenv   # loads .env so students don't paste keys into code
